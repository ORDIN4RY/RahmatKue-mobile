<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="#E0E0E0" />  <!-- Warna background (abu-abu) -->
    <corners android:radius="14dp" />  <!-- Sudut membulat -->
    <stroke
        android:width="1dp"
        android:color="#CFCFCF" />      <!-- Border tipis (opsional) -->
    <padding
        android:left="12dp"
        android:top="8dp"
        android:right="12dp"
        android:bottom="8dp"/>
</shape>
